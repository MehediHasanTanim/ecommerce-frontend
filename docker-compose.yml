services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5177:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:8015/api/v1/
    restart: always
